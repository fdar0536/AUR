diff --git a/source/CMakeLists.txt b/source/CMakeLists.txt
index 822cfd492..0a572aa24 100644
--- a/source/CMakeLists.txt
+++ b/source/CMakeLists.txt
@@ -559,10 +559,10 @@ if(NOT MSVC)
     set_target_properties(x265-static PROPERTIES OUTPUT_NAME x265)
 endif()
 if(EXTRA_LIB)
-    target_link_libraries(x265-static ${EXTRA_LIB})
+    target_link_libraries(x265-static ${EXTRA_LIB} ${CMAKE_DL_LIBS})
 endif()
 if(ENABLE_LIBVMAF)
-    target_link_libraries(x265-static ${VMAF})
+    target_link_libraries(x265-static ${VMAF} ${CMAKE_DL_LIBS})
 endif()
 if(SVTHEVC_FOUND)
     target_link_libraries(x265-static ${SVT_HEVC_LIBRARY})
@@ -633,9 +633,9 @@ if(ENABLE_SHARED)
                    ${X265_RC_FILE} $<TARGET_OBJECTS:encoder> $<TARGET_OBJECTS:common>)
     endif()
     if(EXTRA_LIB)
-        target_link_libraries(x265-shared ${EXTRA_LIB})
+        target_link_libraries(x265-shared ${EXTRA_LIB} ${CMAKE_DL_LIBS})
     endif()
-    target_link_libraries(x265-shared ${PLATFORM_LIBS})
+    target_link_libraries(x265-shared ${PLATFORM_LIBS} ${CMAKE_DL_LIBS})
     if(SVTHEVC_FOUND)
         target_link_libraries(x265-shared ${SVT_HEVC_LIBRARY})
     endif(SVTHEVC_FOUND)
@@ -798,9 +798,9 @@ if(ENABLE_CLI)
         if(WIN32 OR NOT ENABLE_SHARED OR INTEL_CXX)
             # The CLI cannot link to the shared library on Windows, it
             # requires internal APIs not exported from the DLL
-            target_link_libraries(cli x265-static ${PLATFORM_LIBS})
+            target_link_libraries(cli x265-static ${PLATFORM_LIBS} ${CMAKE_DL_LIBS})
         else()
-            target_link_libraries(cli x265-shared ${PLATFORM_LIBS})
+            target_link_libraries(cli x265-shared ${PLATFORM_LIBS} ${CMAKE_DL_LIBS})
         endif()
     endif()
     set_target_properties(cli PROPERTIES OUTPUT_NAME x265)
